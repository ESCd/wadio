<a @attributes="@AdditionalAttributes" class=@ClassNames.Combine(AdditionalAttributes, "drop-shadow-sm ease-in flex flex-col group justify-between no-underline p-2.5 ring-0 ring-fuchsia-800/20 rounded-md space-y-2.5 transition w-fit hover:bg-mantle hover:ring-1 hover:shadow-sm", Station is null ? "animate-pulse" : "") href=@(Station is null ? "#" : $"/station/{Station.Id}")>
    <StationArt class="size-40 group-hover:drop-shadow-md" IsInteractive="true" Station="@Station" />
    @if (Station is not null)
    {
        <Marquee class="ease-in max-w-40 text-gray-400 text-sm transition w-full group-hover:!underline hover:underline" Mode="@(MarqueeMode.Active | MarqueeMode.Hover)" Speed="@MarqueeSpeed.Slow" Text="@Station.Name" title="@Station.Name" />
    }
    else
    {
        <div class="bg-crust opacity-85 rounded-sm shadow-xs shrink-0 w-full">&nbsp;</div>
    }
</a>

@code {

    [Parameter(CaptureUnmatchedValues = true)]
    public IReadOnlyDictionary<string, object>? AdditionalAttributes { get; init; }

    [EditorRequired]
    [Parameter]
    public Station? Station { get; init; }

}