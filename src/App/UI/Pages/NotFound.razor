@using Wadio.App.UI.Components.Layout

@page "/not-found"

<AppLayout>
    <div class="grid h-full items-center justify-center p-0.5 pb-2.5 w-full">
        <div class="flex flex-col gap-2.5 items-center justify-center">
            <Icon Name="@IconName.SentimentDissatisfied" Size="@TextSize.ExtraLarge6" />
            <p class="font-semibold text-lg">Not Found</p>
        </div>
    </div>
</AppLayout>