name: screens

on:
  release:
    types: [published]
  workflow_dispatch:

env:
  DOTNET_NOLOGO: true
  NUGET_PACKAGES: ${{ github.workspace }}/.nuget/packages

jobs:
  screens:
    permissions:
      contents: write
      pull-requests: write

    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          fetch-tags: true

      - name: setup node
        uses: actions/setup-node@v6
        with:
          cache: npm
          cache-dependency-path: ./**/package-lock.json
          node-version: 24.x

      - name: setup dotnet
        uses: actions/setup-dotnet@v5
        with:
          cache: true
          cache-dependency-path: src/**/packages.lock.json
          global-json-file: global.json

      - name: trust dev-certs
        run: dotnet dev-certs https --trust

      - name: install workloads
        run: dotnet workload restore && dotnet workload install wasm-tools

      - name: restore
        run: dotnet restore --locked-mode

      - name: capture screens
        run: dotnet run --project tool/ScreenCap/ScreenCap.csproj --no-restore

      - name: upload artifact
        uses: actions/upload-artifact@v5
        with:
          name: screens
          path: tool/ScreenCap/data

      - name: update screens
        run: rm -rf content/screens && mv tool/ScreenCap/data content/screens

      - name: commit screens
        uses: devops-infra/action-commit-push@v1
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          commit_prefix: "[AUTO]"
          commit_message: "Update screenshots"
          target_branch: feature/update-screens

      - name: create pr
        uses: devops-infra/action-pull-request@v1
        with:
          body: "**Automated pull request**<br><br>Updated screenshots:<br>"
          github_token: ${{ secrets.GITHUB_TOKEN }}
          label: documentation, screens
          assignee: cryptoc1
          source_branch: feature/update-screens
          target_branch: main
          template: .github/PULL_REQUEST_TEMPLATE/screens.md
          title: Update screenshots
